name: GitHub Actions Build and Deploy Demo

sudo: false
language: node_js
node_js:
  - 12.22.1
cache: yarn

install:
  - yarn install

script:
  - yarn test
  - yarn build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on: 
    branch: master
